spring:
  config:
    activate:
      on-profile: local

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/our_home;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: